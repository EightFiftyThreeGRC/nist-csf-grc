// js/testing.js — REMOVED 2026-04-27.
//
// The Control Assessment workspace (tab-tester) and its 4-step wizard were
// removed. The underlying assessment data model — state.authBoundaries,
// state.assessmentPlans, state.atoDecisions — is preserved in core.js so
// existing programs keep their data, and the AO can still record an
// authorization decision via openAtoDecisionModal() in js/authorization.js.
//
// This file is intentionally empty. The <script src="js/testing.js"> tag
// has been removed from index.html; the file is kept only so external links
// referencing it (snapshots, exports, archived doc snippets) don't 404.
